Tile Trip — bản Web standalone

Cách chạy (cần HTTP, không double-click index.html):

  npx serve .
  # hoặc: python3 -m http.server 8080

Rồi mở http://localhost:3000 (serve) hoặc cổng bạn chọn.

Folder này đủ: index.html + bundle-gate.js + game.css + assets/
